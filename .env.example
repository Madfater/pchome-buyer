# MongoDB 連線字串（本機開發沒有 docker-compose 時，需自行啟動一個 mongod，
# 例如 docker run -d -p 127.0.0.1:27017:27017 -v pchome-mongo-data:/data/db mongo:7）
MONGO_URI=mongodb://localhost:27017
MONGO_DB=pchome_buyer

# CVC/AUTO_PAY 已移至面板的「設定」視窗，不再透過 .env 設定。
# 若偵測到舊版 .env 仍有 CVC/AUTO_PAY，settings 集合第一次建立時會自動搬進資料庫一次。
